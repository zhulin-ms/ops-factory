# Claude Rules

## Directory Permissions

- `ops-factory/` is the project directory. Files within this directory **can be modified**.
- `goose/` (`/Users/buyangnie/Documents/GitHub/goose`) is a reference directory. Files within this directory **must NOT be modified** — read-only for reference purposes.
